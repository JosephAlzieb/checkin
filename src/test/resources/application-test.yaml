spring:
  flyway:
    schemas: chicken
    create-schemas: true
    url: jdbc:h2:mem:db;MODE=MYSQL;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE;DB_CLOSE_DELAY=-1
    user: sa
    password: sa
  datasource:
    url: jdbc:h2:mem:db;MODE=MYSQL;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password: sa